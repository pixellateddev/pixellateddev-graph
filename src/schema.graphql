### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  tokenType: String!
  user: User!
}

type Course {
  courseName: String!
  currentlyPersuing: Boolean!
  endYear: String
  id: ID!
  instituteName: String!
  location: String
  score: String!
  startYear: String!
}

type Info {
  code: Int!
  message: String!
  status: String!
}

type Job {
  currentlyWorking: Boolean!
  description: String
  endDate: String
  id: ID!
  orgName: String!
  role: String!
  startDate: String!
}

type Mutation {
  addCourse(course: NewCourseInput!, resumeId: ID!): Resume!
  addJobExperience(newJob: NewJobInput!, resumeId: ID!): Resume!
  createResume(title: String!): Resume!
  deleteCourse(courseId: ID!, resumeId: ID!): Resume!
  deleteJobExperience(jobId: ID!, resumeId: ID!): Resume!
  deleteResume(resumeId: ID!): Info!
  editPersonalDetails(personalDetails: PersonalDetailsInput!, resumeId: ID!): Resume!
  login(email: String!, password: String!): AuthPayload!
  signup(email: String!, password: String!): AuthPayload!
  updateCourse(course: NewCourseInput!, courseId: ID!, resumeId: ID!): Resume!
  updateJobExperience(job: NewJobInput!, jobId: ID!, resumeId: ID!): Resume!
}

input NewCourseInput {
  courseName: String!
  currentlyPersuing: Boolean!
  endYear: String
  instituteName: String!
  location: String
  score: String!
  startYear: String!
}

input NewJobInput {
  currentlyWorking: Boolean!
  description: String
  endDate: String
  orgName: String!
  role: String!
  startDate: String!
}

type PersonalDetails {
  currentRole: String
  email: String!
  fullName: String!
  github: String
  linkedin: String
  location: String!
  phoneNumber: String!
  website: String
}

input PersonalDetailsInput {
  currentRole: String
  email: String!
  fullName: String!
  github: String
  linkedin: String
  location: String!
  phoneNumber: String!
  website: String
}

type Query {
  me: User
  resume(resumeId: ID!): Resume
  resumes: [Resume!]!
  user(id: String!): User
  users: [User]!
}

type Resume {
  educationDetails: [Course!]
  id: ID!
  personalDetails: PersonalDetails
  title: String!
  userId: String!
  workExperience: [Job!]
}

type User {
  email: String!
  id: String!
  username: String
}