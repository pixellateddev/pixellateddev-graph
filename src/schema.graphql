### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  tokenType: String!
  user: User!
}

type Course {
  courseName: String!
  currentlyPersuing: Boolean!
  endYear: String
  id: ID!
  instituteName: String!
  location: String
  score: String!
  startYear: String!
}

input CourseInput {
  courseName: String!
  currentlyPersuing: Boolean!
  endYear: String
  instituteName: String!
  location: String
  score: String!
  startYear: String!
}

type Info {
  code: Int!
  message: String!
  status: String!
}

type Job {
  currentlyWorking: Boolean!
  description: String
  endDate: String
  id: ID!
  orgName: String!
  role: String!
  startDate: String!
}

input JobInput {
  currentlyWorking: Boolean!
  description: String
  endDate: String
  orgName: String!
  role: String!
  startDate: String!
}

type Mutation {
  addCourse(course: CourseInput!, resumeId: ID!): Resume!
  addJobExperience(newJob: JobInput!, resumeId: ID!): Resume!
  addSkill(resumeId: ID!, skill: SkillInput!): Resume!
  createResume(title: String!): Resume!
  deleteCourse(courseId: ID!, resumeId: ID!): Resume!
  deleteJobExperience(jobId: ID!, resumeId: ID!): Resume!
  deleteResume(resumeId: ID!): Info!
  deleteSkill(resumeId: ID!, skillId: ID!): Resume!
  editPersonalDetails(personalDetails: PersonalDetailsInput!, resumeId: ID!): Resume!
  login(email: String!, password: String!): AuthPayload!
  signup(email: String!, password: String!): AuthPayload!
  updateCourse(course: CourseInput!, courseId: ID!, resumeId: ID!): Resume!
  updateJobExperience(job: JobInput!, jobId: ID!, resumeId: ID!): Resume!
  updateSkill(resumeId: ID!, skill: SkillInput!, skillId: ID!): Resume!
}

type PersonalDetails {
  currentRole: String
  email: String!
  fullName: String!
  github: String
  linkedin: String
  location: String!
  phoneNumber: String!
  website: String
}

input PersonalDetailsInput {
  currentRole: String
  email: String!
  fullName: String!
  github: String
  linkedin: String
  location: String!
  phoneNumber: String!
  website: String
}

type Query {
  me: User
  resume(resumeId: ID!): Resume
  resumes: [Resume!]!
  user(id: String!): User
  users: [User]!
}

type Resume {
  educationDetails: [Course!]
  id: ID!
  personalDetails: PersonalDetails
  skills: [Skill!]
  title: String!
  userId: String!
  workExperience: [Job!]
}

type Skill {
  description: String
  id: ID!
  proficiency: Int!
  skill: String!
}

input SkillInput {
  description: String
  proficiency: Int!
  skill: String!
}

type User {
  email: String!
  id: String!
  username: String
}